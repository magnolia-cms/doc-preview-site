[build]
  command = "bash build.sh"
  publish = "build/site"
  secrets_scan_enabled = false
  ignore = "if [ \"$BUILD_REASON\" = \"api\" ] || [ \"$BUILD_REASON\" = \"manual\" ]; then exit 1; else exit 0; fi"