---
title: "Audit"
url: https://docs.magnolia-cms.com/product-docs/6.3/Administration/Monitoring/Audit/
category: Magnolia 6.3
version: 6.3
breadcrumb: DX Core > Administration > Monitoring > Audit
---

# Audit

Audit trail allows an administrator to record user activity in the system. An audit trail typically captures the **who, what, when and where**. The default implementation is based on [Log4j 2](https://logging.apache.org/log4j/2.x/index.html) and the files used for logging can be configured to suit your requirements. Magnolia also provides a Log Viewer tool, located in the Tools menu, [to visualize these and other log files](../../../../RNs-6-4-3/Modules/List-of-modules/Log-Tools-module/#_viewing_and_downloading_log_files).

## [](#_auditlogging_configuration)`auditLogging` configuration

This feature is configured in the `log4j2.xml` file and in the `auditLogging` configuration.

### [](#_defining_loggers)Defining loggers

Here you define the loggers to be used by the audit logging. Here is an example extracted from the application `log4j2.xml` :

```xml
<RollingFile name="sync-log-audit"
             fileName="${magnolia.logs.dir}/magnolia-audit.log"
             filePattern="${magnolia.logs.dir}/magnolia-audit-%i.log"
             append="true">
   <PatternLayout pattern="%d{dd.MM.yyyy HH:mm:ss}%m%n"/>
  <Policies>
    <SizeBasedTriggeringPolicy size="1MB"/>
  </Policies>
  <DefaultRolloverStrategy max="5"/>
</RollingFile>
<Async name="log-audit">
  <AppenderRef ref="sync-log-audit"/>
</Async>
```

| Property | Description |
| --- | --- |
| `name` | **required** (type: `String` ) A unique but arbitrary identifier node name for the configuration. It’s used to match an incoming logging request’s `action` to its specific configuration. |
| `logName` | **required** (type: `String` ) Defines the name of the logger that receives and writes the audit log message. |
| `active` | **optional** (type: `boolean` ), *default is false* A flag that enables or disables this specific log configuration. |
| `sendEvents` | **optional** (type: `boolean` ), *default is false* If set to `true` , an `info.magnolia.audit.AuditLoggingManager.AuditLoggingEvent` is fired on Magnolia’s event bus each time a log entry is made. This allows other parts of the system, such as a custom `AuditLoggingEventHandler` , to listen for and process these events. |
| `separator` | **optional** (type: `String` ), *default is ,* The character or string used to separate the different fields (action, data) within the final log message. If this property isn’t set, a default separator ( `,` a comma) from the `AuditLoggingManager` is used instead. |

| Node name | Value | Type |
| --- | --- | --- |
| 📁 server |  |  |
| …​ |  |  |
| 📁 auditLogging |  |  |
| 📁 logConfigurations |  |  |
| ⸬ excludeWorkspaces |  |  |
| ⬩ Messages workspace | messages | String |
| ⬩ User Based Ranker | userranking | String |
| ⬩ class | info.magnolia.audit.AuditLoggingManager | String |
| ⬩defaultSeparator | , | String |
| …​ |  |  |

| Node name | Value |
| --- | --- |
| 📁 auditLogging |  |
| ⬩ defaultSeparator | , |
| ⬩ class | info.magnolia.audit.AuditLoggingManager |
| 📁 logConfigurations |  |
| ⸬ includeMetadata |  |
| ⬩ template | mgnl:template ** |
