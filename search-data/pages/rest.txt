---
title: "REST module"
url: https://docs.magnolia-cms.com/rest/
category: Modules
version: modules
breadcrumb: REST module
---

# REST module

| Edition | CE |
| --- | --- |
| License | [MLA](https://www.magnolia-cms.com/mla.html) , [GPL](http://www.gnu.org/licenses/gpl-3.0.html) |
| Issues | [MGNLREST](https://magnolia-cms.atlassian.net/browse/MGNLREST) |
| Git | Modules / [rest](https://gitlab.magnolia-platform.com/pd/modules/rest) |
| Maven site | [REST](https://nexus.magnolia-cms.com/service/rest/repository/browse/magnolia.public.releases/info/magnolia/rest/) |
| Latest | 4.0.1 |

## [](#_modules)Modules

The [Java API for RESTful Web Services - JAX-RS](https://en.wikipedia.org/wiki/Java_API_for_RESTful_Web_Services) is defined in the packages `javax.ws.rs` and `javax.xml bind`. These are interfaces and sufficient for endpoint classes during compilation. However, on runtime, when the REST resources are used, a webapp also requires implementations of the these two mentioned packages. Magnolia uses [RESTeasy](http://resteasy.jboss.org/) for this purpose.

The dependencies (for both the interfaces and the implementations) are managed by the `magnolia-rest-integration` module.

Magnolia’s REST web services consist of several modules:

-   REST Services

-   REST Integration

-   REST Content Delivery

-   REST Tools


> **Note:** The preconfigured Magnolia bundles contain the magnolia-rest-services, magnolia-rest-integration and magnolia-rest-content-delivery modules but not magnolia-rest-tools.

### [](#_rest_services)REST Services

The REST Services module installs the following endpoints:

-   `nodes`

-   `properties`

-   `commands`

-   `import`

-   `status`


### [](#_rest_integration)REST Integration

The REST Integration module installs the integration part of REST. The module:

-   Manages the dependencies for the required JAX-RS libraries.

-   Monitors `/config/<module-name>/restEndpoints` for any custom endpoints you want to register. The monitoring mechanism is the same as used for observing registered dialogs, templates and apps.

-   Installs a special servlet `RestDispatcherServlet` which dispatches requests to the individual endpoints registered in configuration.

-   For cloud environments, installs a delivery service and an HTTP servlet request decorator.

-   Lets you define additional providers or [marshallers](https://docs.jboss.org/resteasy/docs/6.2.0.Final/userguide/html/Content_Marshalling_Providers.html) you might need. The providers are responsible for translating the return object into JSON/XML and vice-versa.

-   Installs the default `rest` role that initially prevents access to unauthorized requests.


> **Tip:** Registering interfaces (Features, ExceptionMappers, …​)You can set up a provider:By creating a node in /modules/rest-integration/config/additionalProviders/ using the module version handler.ℹ️ We call them additionalProviders even though a Feature or an ExceptionMapper is not a provider interface per se.Through the Service Provider Interface (SPI) with the META-INF/services/jakarta.ws.rs.ext.Providers.

### [](#_rest_content_delivery)REST Content Delivery

The REST Content Delivery module provides a generic `DeliveryEndpointDefinition` that lets you [configure delivery endpoints](api/delivery-endpoint/) for specific content sources.

> **Important:** This module depends on the rest-integration module.

### [](#_rest_tools)REST Tools

The REST Tools module provides:

-   an API-aware dispatcher of REST requests to dynamically registered endpoints.

-   an API-aware environment-bound delivery service useful for (un)registering Swagger endpoints.

-   the Magnolia *REST Tools* app.


#### [](#_rest_tools_app)REST Tools app

The Magnolia REST Tools app makes use of the [Swagger UI](https://swagger.io/tools/swagger-ui/) that allows you to visualize and interact with the API’s resources without having any of the implementation logic in place.

Swagger UI’s `index.html` reads the `apiBasepath` from a GET parameter, provided by the app. The UI is further edited to match the Magnolia 6 UI theme, hide Swagger’s top-bar, as well as to expand tags and organize them first.

## [](#_installing_with_maven)Installing with Maven

Maven is the easiest way to install the module. Add the following to your bundle:

```xml
<dependency>
  <groupId>info.magnolia.rest</groupId>
  <artifactId>magnolia-rest-integration</artifactId>
  <version>4.0.1</version>
</dependency>
```

```xml
<dependency>
  <groupId>info.magnolia.rest</groupId>
  <artifactId>magnolia-rest-services</artifactId>
  <version>4.0.1</version>
</dependency>
```

```xml
<dependency>
  <groupId>info.magnolia.rest</groupId>
  <artifactId>magnolia-rest-tools</artifactId>
  <version>4.0.1</version>
</dependency>
```

```xml
<dependency>
  <groupId>info.magnolia.rest</groupId>
  <artifactId>magnolia-rest-content-delivery</artifactId>
  <version>4.0.1</version>
</dependency>
```

```xml
<dependency>
  <groupId>info.magnolia.rest</groupId>
  <artifactId>magnolia-rest-jcr-delivery</artifactId>
  <version>4.0.1</version>
</dependency>
```

| Property | Description |
| --- | --- |
| `enabledCommands` | **required** Enabled commands node. |
| `<command>` | **required** Arbitrary name for the command. Use any name you like. |
| `access` | **required** Access node. |
| `roles` | **required** Roles node. |
| `<role>` | **required** Role name. Grants the role permission to execute the command. Add the `rest-admin` role. The property name is arbitrary but the value must be a valid role name. |
| `catalogName` | **required** Catalog where the command resides. |
| []() `contextParameters` | **optional** You can configure which keys are used from the context by whitelisting the properties to be returned under `contextParameters.` Those keys and their values are returned as JSON when executing a command. An example configuration is shown below. It allows the keys `request` and `foo` to be returned when executing the Groovy command. See [Whitelist return properties](../product-docs/6.3/Developing/API/Commands-API/#_whitelist_return_properties) for an example curl request for this configuration. /modules/rest-services/rest-endpoints/commands/enabledCommands/groovy Node name Value ⸬ groovy ⬩ commandName groovy ⸬ access ⸬ roles ⬩ rest rest-editor ⸬ contextParameters ⬩ request request ⬩ foo foo | Node name | Value | ⸬ groovy |  | ⬩ commandName | groovy | ⸬ access |  | ⸬ roles |  | ⬩ rest | rest-editor | ⸬ contextParameters |  | ⬩ request | request | ⬩ foo | foo |
| Node name | Value |
| ⸬ groovy |  |
| ⬩ commandName | groovy |
| ⸬ access |  |
| ⸬ roles |  |
| ⬩ rest | rest-editor |
| ⸬ contextParameters |  |
| ⬩ request | request |
| ⬩ foo | foo |
| `commandName` | **required** Command definition name. |

## [](#_usage)Usage

For usage, see the [Delivery API](api/delivery-endpoint/) page.
