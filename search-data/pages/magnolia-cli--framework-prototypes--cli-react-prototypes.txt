---
title: "Magnolia React Component Library"
url: https://docs.magnolia-cms.com/magnolia-cli/framework-prototypes/cli-react-prototypes/
category: CLI
version: general
breadcrumb: Magnolia CLI > Magnolia Component Library > Magnolia React Component Library
---

# Magnolia React Component Library

| Package name | `@magnolia/cli-react-prototypes` |
| --- | --- |
| Repository | [@magnolia/cli-react-prototypes](https://www.npmjs.com/package/@magnolia/cli-react-prototypes) |
| Latest version |  |
| Changelog | [CHANGELOG.md](https://unpkg.com/@magnolia/cli-react-prototypes/CHANGELOG.md) |

## [](#_getting_started)Getting started

1.  Jumpstart a Magnolia project with a React application.

    -   Community Edition

    -   DX Core


    ```console
    npx @magnolia/cli@latest jumpstart -t "headless/react-starter/ce"
    ```

    ```console
    npx @magnolia/cli@latest jumpstart -t "headless/react-starter/dx-core"
    ```

2.  Next, make sure your `mgnl.config.js` is configured to use the Magnolia React Component Library. The most important is the `framework` property used by the `create-page` and `create-component` plugins:

    ```js
    import CreatePagePlugin from "@magnolia/cli-create-page-plugin";
    import CreateComponentPlugin from "@magnolia/cli-create-component-plugin";

    export default {
      ...
      plugins: [
        new CreatePagePlugin({
          pagesSpaPath: './spa/src/templates/pages',
          framework: '@magnolia/cli-react-prototypes', (1)
        }),
        new CreateComponentPlugin({
          componentsSpaPath: './spa/src/templates/components',
          framework: '@magnolia/cli-react-prototypes', (1)
        })
      ],
      ...
    };
    ```

    |     |     |
    | --- | --- |
    | **1** | The NPM package that provides the Magnolia React Component Library. |


## [](#_available_components)Available components

The React Component Library contains a collection of base components. Each component includes:

-   a dialog definition

-   a template definition

-   an implementation


You can customize, extend, or use them as a starting point for your own components.
