---
title: "Create Stories plugin"
url: https://docs.magnolia-cms.com/magnolia-cli/plugins/available-plugins/create-stories-plugin/
category: CLI
version: general
breadcrumb: Magnolia CLI > Plugins > Create Stories plugin
---

# Create Stories plugin

| Package name | `@magnolia/cli-create-stories-plugin` |
| --- | --- |
| Repository link | [@magnolia/cli-create-stories-plugin](https://www.npmjs.com/package/@magnolia/cli-create-stories-plugin) |
| Latest version |  |
| Changelog | [CHANGELOG.md]({cli-create-stories-plugin-npmRepoUrlUnpkg}CHANGELOG.md) |

## [](#_installing)Installing

The plugin doesn’t come pre-installed with Magnolia CLI. You must add it to the `mgnl.config.js` project file.

> **Note:** The plugin is installed automatically by the Jumpstart plugin if it is defined in a project template.

### [](#_with_add_plugin)With add-plugin

Run the following command in the project folder.

```console
npm run mgnl -- add-plugin @magnolia/cli-create-stories-plugin
```

> **Caution:** Windows users using PowerShell must enclose the two consecutive hyphens (--) in quotes:npm run mgnl "--" ...

The command automatically downloads, installs, and registers the plugin in the `mgnl.config.js` file.

### [](#_manually)Manually

1.  Run the following command in the project folder.

    ```console
    npm install @magnolia/cli-create-stories-plugin
    ```

2.  Modify the `mgnl.config.js` file accordingly.

    ```js
    import CreateStoriesPlugin from "@magnolia/cli-create-stories-plugin";

    export default {
      plugins: [
        new CreateStoriesPlugin()
      ]
    };
    ```


| Form (short and long) | Description |
| --- | --- |
| `-lm [name]` `--light-module [name]` | Sets a light module for the stories. By default, uses the directory specified in the `mgnl.config.js` file. |
| `-s [path…​]` `--source [path…​]` | Provide one or more paths to YAML files for specific components or pages to generate stories from. |
| `-v` `--version` | Displays the version. |
| `-h` `--help` | Displays help information. |
