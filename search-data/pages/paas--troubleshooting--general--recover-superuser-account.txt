---
title: "Recover the superuser account"
url: https://docs.magnolia-cms.com/paas/troubleshooting/general/recover-superuser-account/
category: DX Cloud
version: cloud
breadcrumb: DX Cloud > Troubleshoot > Recover the superuser account
---

# Recover the `superuser` account

Have you blocked the `superuser` account? You nay need to use the [groovy rescue app](../rescue-app/) through rescue mode in your `values.yaml` file.

## [](#_instructions)Instructions

1.  Get the current values `NAMESPACE` values.

    ```sh
    helm -n NAMESPACE get values NAMESPACE -o yaml > values.yaml
    ```

2.  Set rescue mode to `true` in `values.yaml`.

    ```yaml
    magnoliaAuthor:
      rescueModeDX: true
    ```

3.  [Deploy](../../../deployment/overview/) the helm chart in rescue mode.

    ```sh
    helm -n NAMESPACE upgrade NAMESPACE mironet/magnolia-helm --version 1.4.5 -f values.yaml
    ```

4.  Use the provided Groovy Script.

    ```groovy
    session = MgnlContext.getJCRSession('users')
    superuser = session.getNode('/system/superuser')
    superuser.pswd = info.magnolia.cms.security.SecurityUtil.getBCrypt('superuser')
    superuser.enabled = true (1)
    session.save()
    ```

    |     |     |
    | --- | --- |
    | **1** | Don’t forget to enable the `superuser`. |

5.  Toggle the value of rescue mode to `false`.

    ```yaml
    magnoliaAuthor:
      rescueModeDX: false
    ```

6.  [Deploy](../../../deployment/overview/) the helm chart again to remove rescue mode.

    ```bash
    helm -n NAMESPACE upgrade NAMESPACE mironet/magnolia-helm --version 1.4.5 -f values.yaml
    ```
