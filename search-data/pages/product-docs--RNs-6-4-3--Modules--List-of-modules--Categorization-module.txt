---
title: "Categorization module"
url: https://docs.magnolia-cms.com/product-docs/RNs-6-4-3/Modules/List-of-modules/Categorization-module/
category: Magnolia 6.4
version: latest
breadcrumb: DX Core > Modules > Categorization module
---

# Categorization module

| Edition | CE |
| --- | --- |
| License | [MLA](https://www.magnolia-cms.com/mla.html) , [GPL](http://www.gnu.org/licenses/gpl-3.0.html) |
| Issues | [MGNLCAT](https://magnolia-cms.atlassian.net/browse/MGNLCAT) |
| Maven site | [Categorization](https://nexus.magnolia-cms.com/#browse/browse:public:info%2Fmagnolia%2Fcategorization%2Fmagnolia-categorization%2F{magnolia-module-categorization-64}) |
| Latest | {magnolia-module-categorization-64} |

| artifactID |  |
| --- | --- |
| `magnolia-module-categorization-parent` | Parent reactor. |
| `magnolia-categorization` | Main module containing the API, commands and the [catfn](../../../Developing/Templating/Template-scripts/Templating-functions/catfn/) templating functions. |
| `magnolia-categorization-support` | Submodule containing the info.magnolia.module.categorization.support.CategorizationSupport interface. |
| `magnolia-categorization-app` | Submodule containing the [Categories app](../../../Apps/List-of-apps/Categories-app/) . |
| `magnolia-categorization-app-compatibility` | Submodule containing the Categories compatibility app. Bundled in `magnolia-community-webapp` and used for backward compatibility. ** For custom bundles, you have to add it yourself. | ** | For custom bundles, you have to add it yourself. |
| ** | For custom bundles, you have to add it yourself. |
| `magnolia-categorization-rss-integration` | Submodule for the [RSS Aggregator module](../RSS-Aggregator-module/) . |

## [](#_installing)Installing

If you are using a [preconfigured Magnolia webapp or bundle](../../../Developing/Bundles-and-webapps/), the modules are already included. The same is true if you are using a custom webapp based on `magnolia-community-webapp` or `magnolia-dx-core-webapp`.

### [](#_installing_with_maven)Installing with Maven

Maven is the easiest way to install the module. Add the following to your [bundle](../../../Developing/Bundles-and-webapps/):

```xml
<dependency>
  <groupId>info.magnolia.categorization</groupId>
  <artifactId>magnolia-categorization</artifactId>
  <version>{magnolia-module-categorization-64}</version> (1)
</dependency>
```

|     |     |
| --- | --- |
| **1** | Should you need to specify the module version, do it using `<version>`. |

`magnolia-categorization` depends on `magnolia-categorization-support`. It is sufficient to add the former to the POM file of your webapp.

## [](#_what_is_a_category)What is a category?

A category is a non-hierarchical keyword assigned to a piece of content to help describe and find that content. Compared to building a hierarchy, categorization is a bottom-up approach. You can classify your content in an unlimited number of ways, and there is no wrong choice. Instead of belonging to only one category, an item may belong to several.

Categorization is typically a prerequisite to faceted search. You first need a faceted classification system before you can combine facets to filter content quickly.

**Example**: Tour types in the Magnolia

[Travel Demo](../../../Getting-started-with-Magnolia/Demo/) include categories such as `active`, `beach` and `cultural`.

![Tour types](../../../_images/6-2-categorizationModule-tourTypes.png)

Categories assigned to a content item are typically displayed as links on the website.

![Categories on site](../../../_images/6-1-categorizationModule-kyotoOnSite.png)

| Field | Description |
| --- | --- |
| **Category name* (required)** | Internal name used to access the category programmatically (for example, with [catfn](../../../Developing/Templating/Template-scripts/Templating-functions/catfn/) functions). |
| **Display name** | Friendly name displayed to users when the category is rendered, for example, as a link on the website. |
| **Importance** | Gives the category more weight. This is used to promote certain categories. |
| **Related categories** | Links to other categories. This helps visitors discover related content. |

## [](#_categorizing_content)Categorizing content

To categorize content, edit the content item’s properties and choose applicable categories.

**Example**: A tour of Kyoto is categorized as `ecotourism` and `offPath` (off the beaten path).

![Edit mode](../../../_images/6-2-categorizationModule-kyotoEditMode.png)

The easiest way to make categorization available to editors is to use the existing `categories` field from the Categorization module. If you configure your dialog using a YAML file, paste the `categories` field into your form definition.

-   Single selection

-   Multiple selection


```yaml
form:
  properties:
    categories:
      label: Categories
      $type: linkField
      buttonSelectNewLabel: Select new ...
      buttonSelectOtherLabel: Select another ...
      datasource:
        $type: jcrDatasource
        workspace: category
        rootPath: /tour-types
```

```yaml
form:
  properties:
    categories:
      label: Select a category
      $type: jcrMultiLinkField
      field:
        $type: linkField
        label: ""
        datasource:
          $type: jcrDatasource
          workspace: category
          rootPath: /tour-types
```

## [](#_getting_content_by_category)Getting content by category

Use the [`catfn` functions](../../../Developing/Templating/Template-scripts/Templating-functions/catfn/) to access categorized content in your templates. This set of templating functions helps you find content in a particular category. A typical use case is to offer the visitor similarly tagged pages, helping them discover additional interesting content.

**Example 1**: List all pages tagged with `travel`.

```xml
[#assign rootPage = cmsfn.asJCRNode(cmsfn.root(content, "mgnl:page"))!]
[#assign travelPages = catfn.getContentByCategory(rootPage, "travel")!]

<ul>
[#list travelPages as page]
   [#assign title = cmsfn.asContentMap(page).title!page.getName()]
    <li>${title}</li>
[/#list]
</ul>
```

**Example 2**: List the categories of the current page.

```xml
<p>This page has the following categories:
[#assign currentNode = cmsfn.asJCRNode(content)]
[#assign myCategories = cmsfn.asContentMapList(catfn.getCategories(currentNode))]
[#list myCategories as cat ]
    ${cat.displayName},
[/#list]
</p>
```

Related topics

-   [Categories app](../../../Apps/List-of-apps/Categories-app/)

-   [`catfn`](../../../Developing/Templating/Template-scripts/Templating-functions/catfn/) templating functions
