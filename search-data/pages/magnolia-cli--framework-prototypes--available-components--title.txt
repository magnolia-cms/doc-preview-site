---
title: "Title"
url: https://docs.magnolia-cms.com/magnolia-cli/framework-prototypes/available-components/title/
category: CLI
version: general
breadcrumb: Magnolia CLI > Magnolia Component Library > Components > Title
---

# Title

The Title component is used to render a headline with customizable size, color, and alignment.

## [](#_preview)Preview

## [](#_fields)Fields

-   `title` (required) - The text displayed as the headline.

-   `size` (required, default: `h1`) - A combobox to select the heading level:

    -   `h1`

    -   `h2`

    -   `h3`

    -   `h4`

    -   `h5`

    -   `h6`


-   `color` (optional) - Text field to define a custom text color in hex format (e.g., `#000000`).

-   `textAlign` (required, default: `left`) - Combobox to choose text alignment:

    -   `left`

    -   `center`

    -   `right`

    -   `justify`



## [](#_behavior)Behavior

-   Renders a headline (`<h1>` to `<h6>`) based on the selected size.

-   Text is styled and aligned according to the selected options.

-   If color is provided, it overrides the default text color.


## [](#_creating_the_component)Creating the component

To create this component in your project, run:

```console
npm run mgnl -- create-component Title -p title
```

> **Note:** This command adds a local copy of the component to your project. You can fully customize it afterward.

## [](#_available_in)Available in

-   [Magnolia Angular Component Library](../../cli-angular-prototypes/)

-   [Magnolia Freemarker Component Library](../../cli-freemarker-prototypes/)

-   [Magnolia React Component Library](../../cli-react-prototypes/)

-   [Magnolia Vue Component Library](../../cli-vue-prototypes/)
