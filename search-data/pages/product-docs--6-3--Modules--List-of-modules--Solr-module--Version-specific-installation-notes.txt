---
title: "Version-specific installation notes"
url: https://docs.magnolia-cms.com/product-docs/6.3/Modules/List-of-modules/Solr-module/Version-specific-installation-notes/
category: Magnolia 6.3
version: 6.3
breadcrumb: DX Core > Modules > Solr module > Version-specific installation notes
---

# Version-specific installation notes

> **Warning:** Magnolia Solr module versions 7.0, 8.0, and 9.0 contain breaking changes in the API and these versions are not binary compatible with versions before 7.0.

## [](#_version_9_0)Version 9.0

> **Note:** This version adds support for Solr server 9.0.

> **Caution:** This version contains changes in solrconfig.xml and managed-schema. Please read the notes below before updating.

### [](#_updating_to_9_0)Updating to 9.0

Use the [managed-schema](https://gitlab.magnolia-platform.com/pd/extensions/solr-search-provider/-/blob/magnolia-solr-search-provider-9.0.0/sample-solr-config-files/managed-schema?ref_type=tags) and [solrconfig.xml](https://gitlab.magnolia-platform.com/pd/extensions/solr-search-provider/-/blob/magnolia-solr-search-provider-9.0.0/sample-solr-config-files/solrconfig.xml?ref_type=tags) configuration files for your Solr core and for the `$SOLR_HOME/server/solr/configsets/magnolia_data_driven_schema_config`.

This is needed to recreate all Solr indexes because of the changes in configuration files and indexing classes. We recommend recreating the Solr core and then re-triggering indexing in Magnolia to do it.

1.  Use the `solrconfig.xml` and `managed-schema` configuration files for the `$SOLR_HOME/server/solr/configsets/magnolia_data_driven_schema_config` Magnolia config set.

2.  Delete the `magnolia` core and create it again.

    ```console
    cd $SOLR_HOME/bin
    ./solr delete -c magnolia
    ./solr create_core -c magnolia -d magnolia_data_driven_schema_configs
    ```

3.  Re-trigger the indexers by changing their `indexed` property to `false`.
